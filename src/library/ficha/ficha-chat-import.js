/* AcuarioNexo · Crear ficha desde texto del Chat */
(function(){
  function anx(){return window.ANX||{}}
  function S(){return anx().LibrarySchema}
  function esc(v){return anx().esc?anx().esc(v):String(v??'').replace(/[&<>"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[m]))}
  function byId(id){return document.getElementById(id)}
  function val(id){return byId(id)?.value||''}
  function msg(text,type){return anx().msg?anx().msg(text,type):`<div class="${type||'notice'}">${esc(text)}</div>`}
  function norm(s){return String(s||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[*#`_\[\](){}]/g,'').replace(/[:：]+$/,'').replace(/\s+/g,' ').trim()}
  const TYPES=[['pez_marino','Pez marino'],['pez_dulce','Pez de agua dulce'],['coral','Coral'],['invertebrado','Invertebrado'],['planta','Planta'],['microfauna','Microfauna'],['producto','Producto'],['medicamento','Medicamento'],['sal','Sal'],['aditivo','Aditivo'],['alimento','Alimento'],['test','Test'],['equipamiento','Equipamiento']];
  function typeOptions(){return TYPES.map(([k,n])=>`<option value="${k}">${esc(n)}</option>`).join('')}
  function parseSourcesRaw(text){const rows=String(text||'').split(/\n+/).map(s=>s.trim()).filter(Boolean);return rows.map((line,i)=>{const url=(line.match(/https?:\/\/[^\s|]+/i)||[])[0]||'';const parts=line.split('|').map(s=>s.trim());let name=parts[0]&&!/^https?:\/\//i.test(parts[0])?parts[0]:'';if(!name&&url){try{name=new URL(url).hostname}catch(_){name=`Fuente ${i+1}`}}return{url,name:name||`Fuente ${i+1}`,used_for:parts[2]||'Ficha creada desde texto del Chat'}}).filter(x=>x.url)}
  function aliasMap(type){const map=new Map();const add=(name,key)=>{const n=norm(name);if(n&&!map.has(n))map.set(n,key)};
    [['Nombre común','title'],['Nombre comun','title'],['Nombre','title'],['Producto','title'],['Nombre científico','scientific_name'],['Nombre cientifico','scientific_name'],['Resumen','summary'],['Descripción','summary'],['Descripcion','summary'],['Etiquetas','tags'],['Tags','tags'],['Fuentes','sources'],['Fuente','sources'],['Bibliografía','sources'],['Bibliografia','sources'],['Temperatura','temperature'],['pH','ph'],['GH','gh'],['KH','kh'],['Salinidad','salinity'],['Nitrato','nitrate'],['Nitratos','nitrate'],['Fosfato','phosphate'],['Fosfatos','phosphate'],['Calcio','calcium'],['Magnesio','magnesium'],['Acuario mínimo','minimum_tank_liters'],['Acuario minimo','minimum_tank_liters'],['Litros mínimos','minimum_tank_liters'],['Litros minimos','minimum_tank_liters'],['Acuario recomendado','recommended_tank_liters'],['Litros recomendados','recommended_tank_liters'],['Tamaño adulto','adult_size_cm'],['Tamano adulto','adult_size_cm'],['Hábitat natural','habitat'],['Habitat natural','habitat'],['Hábitat','habitat'],['Habitat','habitat'],['Entorno natural','natural_environment'],['Distribución','distribution'],['Distribucion','distribution'],['Profundidad','depth_range'],['Alimentación','diet'],['Alimentacion','diet'],['Dieta','diet'],['Comportamiento','behavior'],['Compatibilidad','compatibility'],['Compatibilidad con peces','fish_compatibility'],['Compatibilidad con corales','coral_compatibility'],['Compatibilidad con invertebrados','invertebrate_compatibility'],['Reef safe','reef_safe'],['Salud','health_notes'],['Enfermedades','common_diseases'],['Problemas frecuentes','common_problems'],['Errores frecuentes','common_mistakes'],['Antes de comprar','purchase_recommendations'],['Curiosidades','curiosities'],['Notas para IA','ai_notes'],['Notas para la IA','ai_notes'],['Resumen para usuario','user_summary'],['Agresividad','aggressiveness'],['Territorialidad','territoriality'],['Reproducción','reproduction'],['Reproduccion','reproduction']].forEach(([a,b])=>add(a,b));
    try{S().templateFor(type).forEach(sec=>sec.fields.forEach(f=>{add(f.id,f.id);add(f.label,f.id)}))}catch(_){ }
    return map;
  }
  function numbers(text){return String(text||'').replace(/,/g,'.').match(/-?\d+(?:\.\d+)?/g)||[]}
  function assignData(data,key,text){const value=String(text||'').trim();if(!value)return;
    if(['temperature','ph','gh','kh','salinity','nitrate','phosphate','calcium','magnesium'].includes(key)){const nums=numbers(value);if(key==='nitrate'||key==='phosphate'){data[`${key}_max`]=nums[nums.length-1]||value;return}if(nums[0])data[`${key}_min`]=nums[0];if(nums[1])data[`${key}_max`]=nums[1];else if(nums[0])data[`${key}_max`]=nums[0];return}
    if(/(_cm|_liters|_years|_days|_watts|grams_per_liter)$/.test(key)){const n=numbers(value)[0];data[key]=n?Number(n):value;return}
    data[key]=value;
  }
  function parseFicha(text,type){const aliases=aliasMap(type), blocks={}, urls=[];let current='';String(text||'').split(/\n/).forEach(raw=>{const line=raw.trim();const found=line.match(/https?:\/\/\S+/gi);if(found)found.forEach(u=>urls.push(u.replace(/[.,;]+$/,'')));if(!line||line.startsWith('```'))return;const clean=line.replace(/^[-*•]\s*/,'').trim();const parts=clean.split(/[:：]/);let head='',body='';if(parts.length>1){head=norm(parts.shift());body=parts.join(':').trim()}else head=norm(clean);const key=aliases.get(head);if(key){current=key;if(body)blocks[current]=[blocks[current],body].filter(Boolean).join('\n');return}if(current)blocks[current]=[blocks[current],clean].filter(Boolean).join('\n')});
    const data={}, sections={};let title='',scientific='',summary='',tags=[],sourceLines='';Object.entries(blocks).forEach(([key,value])=>{if(key==='title')title=value;else if(key==='scientific_name')scientific=value;else if(key==='summary')summary=value;else if(key==='tags')tags=String(value).split(/[,;\n]/).map(s=>s.trim()).filter(Boolean);else if(key==='sources')sourceLines=value;else assignData(data,key,value)});
    if(!summary)summary=data.user_summary||data.description||'';sections.summary=summary;let sources=parseSourcesRaw(sourceLines);if(urls.length){sources=sources.concat(urls.map((u,i)=>({name:`Fuente ${i+1}`,url:u,used_for:'Ficha creada desde texto del Chat'})))}
    return{title,scientific_name:scientific,summary,tags,data,sections,sources:S().normalizeSources(sources)};
  }
  window.mostrarCrearFichaDesdeChat=function(){const app=byId('app');if(!app)return;const box=document.createElement('section');box.className='panel library-detail';box.id='chatCreatePanel';box.innerHTML=`<button onclick="document.getElementById('chatCreatePanel')?.remove()">Cerrar</button><h2>Crear ficha desde texto del Chat</h2><div class="notice">Selecciona el tipo, pega la ficha completa y pulsa Crear ficha. Después se abrirá para revisar, guardar y auditar.</div><label>Tipo de ficha</label><select id="chatCreateType">${typeOptions()}</select><label>Ficha completa creada por ChatGPT</label><textarea id="chatCreateText" placeholder="Pega aquí toda la ficha"></textarea><button class="primary" onclick="crearFichaDesdeChat()">Crear ficha</button><div id="chatCreateStatus"></div>`;app.prepend(box);box.scrollIntoView({behavior:'smooth',block:'start'})}
  window.crearFichaDesdeChat=async function(){const box=byId('chatCreateStatus');try{const A=anx(), supabase=A.supabase, state=A.state;if(!state?.user?.id)throw new Error('Sesión no disponible.');const type=val('chatCreateType')||'pez_marino', text=val('chatCreateText');if(!text.trim())throw new Error('Pega primero la ficha completa.');const parsed=parseFicha(text,type);if(!parsed.title)throw new Error('No encontré el nombre común/producto. Usa el título "Nombre común:" o "Nombre:".');if(!parsed.sources.length)throw new Error('No encontré fuentes con URL. Añade el apartado Fuentes.');const row={user_id:state.user.id,title:parsed.title,scientific_name:parsed.scientific_name||null,entry_type:type,status:'review',visibility:'private',summary:parsed.summary||null,sections:parsed.sections,data:parsed.data,tags:parsed.tags,identity_confirmed:true,confidence:null,identify_result:{source:'chat_paste',identity_confirmed:true,title:parsed.title,scientific_name:parsed.scientific_name,entry_type:type},sources:parsed.sources,ai_model:'chat-paste-manual',ai_generated_at:new Date().toISOString(),updated_at:new Date().toISOString()};if(box)box.innerHTML=msg('Creando ficha desde texto...','notice');const{data,error}=await supabase.from('library_entries').insert(row).select('*').single();if(error)throw error;if(window.biblioteca)await window.biblioteca();if(window.formFicha)setTimeout(()=>window.formFicha(data.id),300);if(box)box.innerHTML=msg('Ficha creada. Revisa y audita.','success')}catch(e){if(box)box.innerHTML=msg(e.message,'error')}};
  function injectButton(){if(!/biblioteca/i.test(location.hash||document.body.innerText||'')){}const panel=document.querySelector('.panel .panel-head');if(!panel||document.getElementById('createFromChatBtn'))return;const btn=document.createElement('button');btn.id='createFromChatBtn';btn.className='primary';btn.textContent='Crear ficha desde Chat';btn.onclick=window.mostrarCrearFichaDesdeChat;panel.appendChild(btn)}
  const mo=new MutationObserver(()=>injectButton());mo.observe(document.body,{childList:true,subtree:true});setTimeout(injectButton,500);
})();
