ACUARIONEXO · MODULOS OFICIALES · 23/07/2026

FUENTE DE VERDAD
- Rama de trabajo y publicación: main.
- Supabase es la fuente de datos, autenticación y Storage.

BUILD ACTUAL
- index.html: library-aquarium-import-20260723-1045.
- app-version.json: library-aquarium-import-20260723-1045.
- manifest.webmanifest: library-aquarium-import-20260723-1045.

BIBLIOTECA · CONTRATO Y AUDITORIA
- src/library/core/library-schema.js: esqueleto completo por tipo, incluida la identidad de las fichas Test.
- src/library/core/library-schema-rules.js: auditoría oficial.
- src/library/ficha/ficha-chat-import.js: exige todos los campos, dos fuentes reales y auditoría aprobada antes de insertar.
- src/library/ficha/ficha-actions.js: vuelve a auditar y contiene la entrada única del botón Añadir.
- src/library/inventory/library-inventory-import.js: carga oficial de acuarios, selección de destino y persistencia.

BIBLIOTECA · IMPORTACION AL ACUARIO
- El botón se identifica mediante id estable y no depende de CSS.escape.
- AquariumsCore.loadAquariums es la única carga de acuarios utilizada por el importador.
- LibrarySchema.audit debe aprobar la ficha antes de abrir y antes de guardar.
- inventory_items es el destino persistente de la copia.

PARAMETROS · CATALOGO OFICIAL DE TESTS
- src/parameters/parameters-core.js: carga fichas Test, normaliza parámetros y filtra compatibilidad.
- src/parameters/measurements-advanced.js: selector independiente por cada parámetro semanal, mensual o ICP.
- src/parameters/parameters-manual.js: selector del mismo catálogo en medición puntual.
- aquarium_measurements.method: guarda el test o método utilizado.
- No existen listas paralelas de marcas.

BIBLIOTECA · AUDITORIA INTEGRAL DE FORMULARIOS
- scripts/audit-library-contracts.mjs: prueba todos los tipos y todos los campos.
- npm run library:check: comprobación obligatoria del contrato.
- title, scientific_name, summary y sources son claves superiores; el resto pertenece a data.
- No se inserta una ficha incompleta para corregirla después.

REGLAS
- Una ficha incompleta no entra en Biblioteca.
- Una medición debe conservar el test o método realmente utilizado.
- No se permiten validadores paralelos, hotfix ni patch.

GENERADO, NO EDITAR A MANO
- www/
- android/
- ios/
- node_modules/