(function(){
function rangeFor(parameter,aquariumType){const p=window.MEASUREMENT_SCHEMA?.parameters?.[parameter];return p?.ranges?.[aquariumType]||p?.ranges?.reef||p?.ranges?.marine||p?.ranges?.freshwater||null;}
function analyzeMeasurement(row,aquarium){const type=(aquarium?.aquarium_type||aquarium?.type||'reef');const r=rangeFor(row.parameter_key,type);const value=Number(row.value_numeric);if(!r||!Number.isFinite(value))return{color:'neutral',title:'Lectura guardada',recommendation:'Sin rango automático para este parámetro.',score:0};const[okMin,okMax,warnMin,warnMax]=r;let color='green',title='Correcto',recommendation='Valor dentro del rango objetivo.';if(value<warnMin||value>warnMax){color='red';title='Fuera de rango';recommendation=value<warnMin?'Valor demasiado bajo. Revisar tendencia, test y posibles correcciones.':'Valor demasiado alto. Revisar tendencia, test y posibles correcciones.';}else if(value<okMin||value>okMax){color='yellow';title='Vigilar';recommendation=value<okMin?'Algo bajo para el rango objetivo. Vigilar evolución.':'Algo alto para el rango objetivo. Vigilar evolución.';}return{color,title,recommendation,score:color==='green'?1:color==='yellow'?0:-1};}
function detectAquariumMode(aquarium){const t=String(aquarium?.aquarium_type||aquarium?.subtype||'').toLowerCase();if(t.includes('fresh')||t.includes('dulce')||t.includes('betta')||t.includes('escala'))return'freshwater';return'marine';}
window.AcuarioNexoMeasurementAI={analyzeMeasurement,detectAquariumMode};
})();
